StreamWriter бере ваш звичайний текст і перетворює його на потік байтів, який "затікає" у файл і зберігається там. 
StreamReader робить зворотне: він "витягує" байти з файлу і перетворює їх назад на зрозумілий вам текст.